import { PageWrapper } from './ContactPage.styled';
import type { ContactPageProps } from './ContactPage.types';

const ContactPage = (props: ContactPageProps) => {
  return <PageWrapper {...props} />;
};

export default ContactPage;
