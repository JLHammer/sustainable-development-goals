import { PageWrapper } from './ErrorPage.styled';
import type { ErrorPageProps } from './ErrorPage.types';

const ErrorPage = (props: ErrorPageProps) => {
  return <PageWrapper {...props} />;
};

export default ErrorPage;
