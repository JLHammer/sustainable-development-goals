import { Section } from './GoalList.styled';
import type { GoalListProps } from './GoalList.types';

const GoalList = (props: GoalListProps) => {
  return <Section {...props} />;
};

export default GoalList;
